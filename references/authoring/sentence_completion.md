# Sentence completion

- Write a complete `stem` with a clearly marked completion and state the intended meaning through context, not a hidden key.
- Use `answer.primary` and `answer.accepted` to define grammatical and semantically equivalent responses; specify constraints when needed.
- Check tense, agreement, word order, punctuation, and score against the cited canonical item. The rationale must match the accepted-answer policy.
