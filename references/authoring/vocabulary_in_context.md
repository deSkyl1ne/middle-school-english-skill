# Vocabulary in context

- Provide a short original `context` and a focused `stem` asking for meaning or usage in that context.
- Use parallel distractors where appropriate. Store either one option ID or one explicitly accepted word in `answer`.
- Make the intended sense uniquely recoverable from the context, not from an isolated dictionary fact; explain the decisive cue in `rationale`.
