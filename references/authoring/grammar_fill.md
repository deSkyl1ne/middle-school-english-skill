# Grammar fill

- Give enough context in `stem` to determine the required form or structure and cite the target canonical grammar item.
- Store `answer.primary` plus `answer.accepted` variants; accept variants only when they preserve the intended meaning, tense, agreement, and word order.
- Explain the form selection and reject plausible but incorrect alternatives in `rationale`.
